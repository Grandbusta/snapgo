package routes

import (
	"github.com/gin-gonic/gin"
)

func UserRoutes(g *gin.Engine) {
	// u := g.Group("/")
	// user := actions.NewUserAction(repository.UserRepository)

	// u.GET("/signup", user.CreateUser())

}
